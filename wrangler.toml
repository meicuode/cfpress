name = "cfpress"
compatibility_date = "2025-11-01"

# D1 数据库绑定 (用于本地开发)
[[d1_databases]]
binding = "DB"
database_name = "cfpress"
database_id = "local-db"

# R2 存储桶绑定 (用于文件存储)
[[r2_buckets]]
binding = "FILES"
bucket_name = "cfpress-files"
preview_bucket_name = "cfpress-files-preview"
